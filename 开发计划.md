# WorkLift 开发计划（可直接执行）

> 依据 `PRD.md` 制定。本计划是实施阶段的唯一执行依据；PRD 的"验收标准"（§10）是最终验收清单。
> 环境已确认：Node.js v22.20.0、npm 10.9.3（Windows）。

---

## 0. 技术选型与运行方式

| 项 | 决策 | 理由 |
|---|---|---|
| 运行时 | Node.js（已安装） | 本机可用，无需再装 |
| 后端 | Express + 原生 Node http | 仅需静态文件 + 数据读写，最小依赖 |
| 前端 | 原生 HTML/CSS/JS 单页应用（hash 路由） | 无构建步骤、无外部CDN → 保证"断网可用"（PRD §7） |
| 数据存储 | 单个 JSON 文件 `data/worklift-data.json` | 符合"数据在一个文件中"；备份=复制该文件 |
| 保存方式 | 每次操作 PUT 整个数据对象，服务端**原子写入**（先写临时文件再重命名） | 满足"即时保存、刷新/关闭/重启不丢" |
| 端口 | 8787，仅绑定 127.0.0.1 | 只本机访问，不暴露局域网 |
| 启动 | `start.bat` → `node server.js` → 服务启动后**自动用默认浏览器打开** http://127.0.0.1:8787 | 双击即用（PRD §1 B方案） |
| 依赖 | 仅 `express` 一个 npm 包 | 最小化 |

## 1. 项目结构

```
D:\Blogs\WorkLiftApp\
├── PRD.md                  # 需求依据（已完成）
├── 开发计划.md              # 本文档
├── package.json            # 声明 express 依赖、start 脚本
├── server.js               # 静态服务 + 数据API + 原子保存 + 自动开浏览器
├── start.bat               # 双击启动（cd 到本目录 → node server.js）
├── .gitignore              # 忽略 node_modules、data/（数据属个人，不入库）
├── data\
│   └── worklift-data.json  # 唯一数据文件（首次启动自动创建默认结构）
└── public\                 # 前端（全部本地文件，无CDN）
    ├── index.html          # SPA 外壳：侧边导航 + 内容区
    ├── css\style.css       # 全部样式（含响应式、配色、组件样式）
    └── js\
        ├── api.js          # fetch 封装（getData/saveData/downloadBackup）
        ├── store.js        # 前端数据状态 + 变更即保存（含防抖200ms）
        ├── router.js       # hash 路由：#/home #/today #/dev #/consult #/diet #/money #/settings
        ├── components\
        │   ├── toast.js    # 底部"已保存"轻提示
        │   ├── confirm.js  # 确认弹窗（普通删除=弹窗；恢复/清空=弹窗+输入"确认"两字）
        │   ├── modal.js    # 通用表单弹窗（编辑/新建共用）
        │   ├── dateNav.js  # ◀ ▶ + 日期显示 + 点日期弹日历 + "回到今天"
        │   └── empty.js    # 空状态引导文案组件
        └── pages\
            ├── home.js     # 首页总览
            ├── today.js    # 今日计划
            ├── dev.js      # 开发工作
            ├── consult.js  # 咨询工作
            ├── diet.js     # 饮食计划
            ├── money.js    # 账目计划
            └── settings.js # 数据与设置
```

## 2. 数据模型（data/worklift-data.json）

```jsonc
{
  "version": 1,
  "updatedAt": "2026-08-11T10:00:00+08:00",

  "memos": [                      // 快速备忘（首页）
    { "id": "m1", "text": "记得周五交房租", "pinned": true, "createdAt": "..." }
  ],

  "plans": {                       // 今日计划：键=日期 YYYY-MM-DD
    "2026-08-11": [
      { "id": "p1", "text": "上午改完首页设计", "done": false,
        "important": true, "note": "", "origDate": "2026-08-10" }
    ]
  },

  "projects": [                    // 开发工作
    { "id": "pr1", "name": "WorkLift", "desc": "",
      "tasks": [
        { "id": "t1", "title": "首页设计", "status": "doing",   // todo|doing|done
          "priority": "important", "note": "" }                // normal|important
      ],
      "logs": [                    // 工作日志
        { "id": "l1", "date": "2026-08-11", "content": "做了首页设计", "hours": "2小时" }
      ] }
  ],

  "clients": [                     // 咨询工作
    { "id": "c1", "name": "王总", "phone": "138xxxx",
      "status": "active",          // potential|active|done（潜在/进行中/已结束）
      "note": "",
      "records": [ { "id": "r1", "date": "2026-08-10", "content": "聊了项目报价" } ],
      "todos":   [ { "id": "ct1", "text": "周四回访", "done": false } ],
      "fees":    [ { "id": "f1", "amount": 1200, "received": false, "date": "2026-08-10" } ] }
  ],

  "meals": {                       // 饮食计划：键=日期
    "2026-08-11": {
      "breakfast": { "food": "两个鸡蛋+牛奶", "note": "" },
      "lunch":     null,
      "dinner":    null,
      "snack":     null
    }
  },

  "ledger": [                      // 账目计划
    { "id": "e1", "type": "expense", "amount": 35,   // expense|income
      "category": "餐饮", "date": "2026-08-11", "note": "午餐" }
  ],

  "categories": [                  // 账目分类（默认分类 builtin=true 不可删）
    { "name": "餐饮", "builtin": true }, { "name": "交通", "builtin": true },
    { "name": "购物", "builtin": true }, { "name": "住房", "builtin": true },
    { "name": "医疗", "builtin": true }, { "name": "其他", "builtin": true },
    { "name": "工资收入", "builtin": true }
    // 自定义分类：{ "name": "xxx", "builtin": false }
  ]
}
```

**关键实现规则**
- 所有日期用本地日期字符串 `YYYY-MM-DD`（不用UTC，避免时区差一天）。
- **逾期顺延是"算出来的视图"，不改存储**：看"今天"时，取今天的任务 + 所有 `origDate < 今天 && !done` 的任务，顺延项标记"昨·未完成"并显示原日期。"逾期"标签 = 所有 `date < 今天 && !done` 的任务（今天视图中顺延来的）。
- 原子保存：服务端把新数据写 `worklift-data.json.tmp` → 成功后重命名覆盖原文件。
- 服务端校验：PUT 前检查是合法 JSON 且含 `version`；非法则拒绝（400）。
- 分类下拉只显示支出类给"支出"、收入类给"收入"（默认分类合计覆盖 PRD 的7个名称）。

## 3. API 设计

| 接口 | 方法 | 说明 |
|---|---|---|
| `/`、`/index.html`、`/css/*`、`/js/*` | GET | 静态文件（前端资源） |
| `/api/data` | GET | 返回整个数据对象 |
| `/api/data` | PUT | 接收整个数据对象，校验后原子保存；成功返回 `{ok:true}` |
| `/api/backup` | GET | 以附件下载数据文件，文件名 `WorkLift备份-YYYY-MM-DD.json`（浏览器自动弹"另存为"选位置） |
| `/api/ping` | GET | 返回 `{ok:true}`（启动自检用） |

恢复=前端选择备份文件 → 校验 JSON → 确认框输入"确认" → PUT 覆盖。无需单独接口。

## 4. 阶段划分与依赖关系

| 阶段 | 内容 | 依赖 | 验收方式 |
|---|---|---|---|
| P0 | 环境与骨架（server + start.bat + 结构） | 无 | 双击启动、curl、重启持久 |
| P1 | 数据层（schema、API、store） | P0 | curl 读写、重启持久 |
| P2 | 应用外壳（导航、路由、通用组件、样式） | P1 | 浏览器7页切换、控制台无错 |
| P3 | 今日计划模块 | P2 | 浏览器按 PRD §5.2 操作 |
| P4 | 开发工作模块 | P2 | 浏览器按 PRD §5.3 操作 |
| P5 | 咨询工作模块 | P2 | 浏览器按 PRD §5.4 操作 |
| P6 | 饮食计划模块 | P2 | 浏览器按 PRD §5.5 操作 |
| P7 | 账目计划模块 | P2 | 浏览器按 PRD §5.6 操作 |
| P8 | 首页总览 | P3–P7 | 浏览器核对摘要与各模块一致 |
| P9 | 数据与设置 | P2 | 浏览器备份/恢复/清空/分类 |
| P10 | 全量验收 | P0–P9 | 按 PRD §10 逐条勾选 |

> **并行执行**：P3–P7 与 P9 互不依赖（各自页面 + 各自数据切片），可在 P2 完成后并行开发。P8 依赖 P3–P7 全部完成。P10 最后统一验收。
> 前端页面文件均独立（pages/*.js），并行时无文件冲突；仅 `store.js`/`api.js`/`style.css` 由 P1/P2 定稿后共用，并行阶段**只读不修改**。

---

## 阶段 P0：环境与骨架

**任务**
- [ ] `package.json`：`npm init -y` 后设置 `"scripts": {"start": "node server.js"}`，安装 `express`
- [ ] 创建目录：`data\`、`public\css\`、`public\js\components\`、`public\js\pages\`
- [ ] `.gitignore`：`node_modules/`、`data/`
- [ ] `server.js`：
  - 静态托管 `public/`
  - 绑定 `127.0.0.1:8787`
  - 首次启动时若无 `data/worklift-data.json` 则写入默认结构（见 §2）
  - `listen` 成功后用 `child_process.exec('start http://127.0.0.1:8787')` 自动打开默认浏览器
- [ ] `start.bat`：`@echo off` + `cd /d %~dp0` + `node server.js`
- [ ] `public/index.html` 最小骨架：标题 + 空内容区（P2 再填导航）
- [ ] `public/js/api.js`：`getData() / saveData(obj) / downloadBackup()` 封装（fetch 到 §3 接口）

**验收（P0）**
- [ ] 双击 `start.bat` → 弹出命令行窗口 → 浏览器自动打开 http://127.0.0.1:8787 显示页面
- [ ] 命令行显示监听信息，无报错
- [ ] `data\worklift-data.json` 已自动创建
- [ ] `curl http://127.0.0.1:8787/api/data` 返回默认结构 JSON

## 阶段 P1：数据层

**任务**
- [ ] `server.js` 增加 `GET /api/data`（读文件返回）、`PUT /api/data`（校验+原子写+更新 `updatedAt`）、`GET /api/backup`（附件下载）、`GET /api/ping`
- [ ] PUT 校验：body 必须是 JSON 对象、含 `version` 字段；否则 400 且不写文件
- [ ] 原子写：写 `worklift-data.json.tmp` → `fs.renameSync` 覆盖；异常时保留原文件
- [ ] `public/js/store.js`：加载数据到内存、`save()` 防抖200ms 调 PUT、`getState()` 供页面读取；页面操作后调 `markDirty()`

**验收（P1）**
- [ ] `curl -X PUT http://127.0.0.1:8787/api/data -H "Content-Type: application/json" -d @<测试json>` 返回 `{"ok":true}`
- [ ] PUT 非法 JSON（如 `{}` 无 version）返回 400 且文件未被破坏
- [ ] 写入后重启 server，`GET /api/data` 仍返回刚写入的内容（持久化）
- [ ] 修改 `data\worklift-data.json` 的 `updatedAt` 会被 PUT 刷新

## 阶段 P2：应用外壳

**任务**
- [ ] `index.html`：左侧导航（7项，含图标与文字，PRD §2 结构）+ 内容区 `<div id="app">`
- [ ] `router.js`：hash 路由，默认 `#/home`；导航点击切页并高亮；页面切换保留各页状态（单页应用）
- [ ] `style.css`：整体配色（完成/已收=绿、重要/待收/逾期=橙/红）、卡片、按钮、表单、弹窗、toast、导航栏；窄窗口自动换行（不做手机版）
- [ ] `components/modal.js`：通用弹窗（标题 + 表单字段 + 确定/取消），供各模块编辑/新建复用
- [ ] `components/confirm.js`：两种确认——普通（确定/取消）；危险（警告文案 + 必须输入"确认"两字，按钮才可点）
- [ ] `components/toast.js`：底部轻提示"已保存"，1.5秒自动消失
- [ ] `components/empty.js`：`empty('还没有X，点这里开始')`
- [ ] `components/dateNav.js`：◀ ▶ + 日期文本 + 点日期弹原生日期选择器 + "回到今天"按钮
- [ ] 7 个 `pages/*.js` 先放占位内容（标题+空状态），保证路由可切换

**验收（P2）**
- [ ] 浏览器中 7 个导航项全部可切换，当前页高亮，hash 变化正确
- [ ] 刷新页面后仍停留在当前模块（hash 路由）
- [ ] 控制台无报错；断网刷新页面资源全部来自本机

## 阶段 P3：今日计划（PRD §5.2 / §8）

**任务**
- [ ] 顶部 `dateNav` + 添加框（回车即添加，默认加到当前查看日期）
- [ ] 列表项：复选框（勾选→done、划掉并沉到底部、可取消）、⭐重要（橙）、✏编辑、🗑删除（确认弹窗）
- [ ] 点任务展开编辑：内容/日期/优先级/备注（用 modal）
- [ ] 标签筛选：全部 / 今天 / 明天 / 逾期
- [ ] **顺延逻辑**：看"今天"时合并 `origDate<今天 && !done` 项并标"昨·未完成（原X月X日）"；"逾期"标签显示所有未完成过期项
- [ ] 空状态："今天还没有安排，写第一件事吧"

**验收（P3）**：见 PRD §10「今日计划」5 条，逐条通过；顺延任务刷新后仍在。

## 阶段 P4：开发工作（PRD §5.3 / §8）

**任务**
- [ ] 左栏项目列表 + "新建项目"（名称、说明）；每项显示"待办X · 进行中X"
- [ ] 项目编辑/删除（删除确认框）
- [ ] 右侧三栏看板：待办/进行中/已完成；每栏顶部"＋加任务"
- [ ] 任务：→ 按钮推下一栏（done 不往后推）；点任务展开编辑标题/优先级/备注；删除
- [ ] 工作日志区：新在上，字段=日期（默认今天）+内容+耗时；可删除

**验收（P4）**：见 PRD §10「开发工作」4 条，逐条通过。

## 阶段 P5：咨询工作（PRD §5.4 / §8）

**任务**
- [ ] 左栏客户列表 + "新建客户"（姓名/称呼、电话、状态三选、备注）
- [ ] 状态彩色标签：潜在=灰、进行中=蓝、已结束=绿
- [ ] 客户编辑/删除（删除确认框）
- [ ] 右侧三个标签页：咨询记录 / 待办事项 / 费用记录
  - 记录：日期+内容，"＋记一条咨询"，可删
  - 待办：勾选列表，可增删
  - 费用：金额+日期，可增删改；"标记已收"按钮；顶部汇总"已收 ¥X · 待收 ¥Y"
- [ ] 空客户选中前显示"选择左侧客户"

**验收（P5）**：见 PRD §10「咨询工作」4 条，逐条通过。

## 阶段 P6：饮食计划（PRD §5.5 / §8）

**任务**
- [ ] 顶部 `dateNav` + 进度点（4餐：记=实心●、没记=空心○）
- [ ] 4 个餐位格子：早餐/午餐/晚餐/加餐；"＋记录"弹小框（吃了什么 + 可选备注）
- [ ] 已记录可编辑、删除
- [ ] 看今天时：若最近3天（含今天）无任何记录，显示灰色提示"最近没记录饮食"

**验收（P6）**：见 PRD §10「饮食计划」3 条，逐条通过。

## 阶段 P7：账目计划（PRD §5.6 / §8）

**任务**
- [ ] "＋记一笔"弹窗：支出/收入 切换 → 金额 → 分类下拉（支出显示支出类、收入显示收入类）→ 日期（默认今天）→ 备注
- [ ] 顶部三卡：本月收入 / 本月支出 / 本月结余
- [ ] 类别条形：本月支出按分类求和降序，横向条，最长排最上
- [ ] 明细列表（日期倒序）：筛选 全部/支出/收入/分类；每行可编辑、删除
- [ ] `dateNav` 按**月份**切换（◀ 2026年8月 ▶）

**验收（P7）**：见 PRD §10「账目计划」5 条，逐条通过。

## 阶段 P8：首页总览（PRD §3 / §8）

**任务**（依赖 P3–P7 的数据）
- [ ] 顶部：今天日期 + "今天X件事、完成Y件"（含顺延项）
- [ ] 5 张摘要卡片（可点击跳转）：
  - 今日计划：完成 X/Y（今天+顺延）
  - 开发工作：所有项目 doing 数合计 + 今天日志条数
  - 咨询工作：本周（周一至周日）咨询记录次数 + 全部未收费用合计
  - 饮食计划：今天记了几餐
  - 账目计划：今天支出合计 + 本月结余
- [ ] 快速备忘区：输入添加、⭐置顶、删除；置顶排最上
- [ ] 每次进入首页或数据变化时刷新摘要

**验收（P8）**：见 PRD §10「首页」3 条 + 「启动与导航」第3条，逐条通过；各卡片数字与对应模块实际操作结果一致。

## 阶段 P9：数据与设置（PRD §5.7 / §8）

**任务**
- [ ] 数据文件信息卡片：位置（绝对路径）、大小（KB）、`updatedAt` 显示"上次保存"
- [ ] 备份：`downloadBackup()` 触发浏览器"另存为"，默认文件名 `WorkLift备份-YYYY-MM-DD.json`
- [ ] 恢复：文件选择 → 校验是合法数据 JSON → 危险确认（输"确认"）→ PUT 覆盖 → 刷新界面
- [ ] 账目分类管理：列表显示所有分类，默认分类显示"默认"不可删；自定义分类可删（若已被账目使用，提示仍会删除？→ 决策：**不可删被使用的自定义分类**，提示"该分类下已有账目，请先改账目"）；"＋添加"输入新分类名，重名拒绝
- [ ] 清空全部数据：两次确认（第一次确认框 → 第二次弹窗输入"确认"）→ PUT 空默认结构

**验收（P9）**：见 PRD §10「数据与设置」5 条 + 「数据不丢失」第4条（断网），逐条通过。

## 阶段 P10：全量验收

**任务**
- [ ] 按 `PRD.md` §10 全部验收清单逐条实际操作勾选
- [ ] 每个勾选失败项：修复后重测，直至全绿
- [ ] 重启电脑流程实测：重启后双击 `start.bat` → 数据完整
- [ ] 备份文件内容与当前数据一致（用文本编辑器打开核对关键字段）

**完成标准**：PRD §10 全部 `- [ ]` 变为已勾选；P0–P9 各阶段验收项全部通过。

---

## 5. 执行顺序建议

1. **P0 → P1 → P2**：串行（骨架与数据层是一切的基础，且共用文件）
2. **P2 完成后，P3、P4、P5、P6、P7、P9 并行**（6 个独立模块，页面文件互不冲突）
3. **P3–P7 全部完成后 → P8**（首页需要所有模块的摘要）
4. **全部完成后 → P10** 全量验收

## 6. 风险与注意点

- **端口冲突**：若 8787 被占用，`server.js` 打印明确错误并给出改端口提示（改一处常量即可）。
- **原子写失败**：磁盘满等极端情况，保留原文件并返回 500，前端 toast 提示"保存失败，请重试"。
- **时间与日期**：一律用本地日期 `YYYY-MM-DD`；勿用 `toISOString()`（是UTC，可能差一天）。
- **断网**：前端零 CDN；`npm install` 只在开发时联网一次。
- **中文文件名备份**：`Content-Disposition` 需 `filename*=UTF-8''...` 编码，否则中文名乱码。
- **不要做**：任何 PRD §9 暂缓功能（提醒、图表、日历视图等），验收时若发现超出范围一律移除。
